# Ansible

This directory contains automation used to enforce baseline configuration and prevent drift.

Conventions:
- Inventory files live here
- Playbooks must be idempotent
- Include verification tasks where possible
